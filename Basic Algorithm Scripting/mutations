function mutation(arr) {
  
  let array_1 = arr[0].toLowerCase().split("");
  let array_2 = arr[1].toLowerCase().split("");

  for(let i=0;i<array_2.length;i++) {
    if ( ! array_1.includes(array_2[i]) )
      return false;  

  }
  
  return true;
}

console.log (mutation(["hello", "Hello"]));
